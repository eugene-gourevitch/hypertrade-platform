# =============================================================================
# HYPERTRADE PLATFORM - PRODUCTION ENVIRONMENT VARIABLES
# Copy these to your Vercel Environment Variables dashboard
# https://vercel.com/your-project/settings/environment-variables
# =============================================================================

# Server Configuration (Vercel handles this automatically)
NODE_ENV=production

# Hyperliquid Network
# Set to 'true' for testnet, 'false' for mainnet (real money!)
HYPERLIQUID_TESTNET=false

# Wallet Authorization
# Set to 'none' to allow any wallet to connect
# Or set to a specific wallet address: 0x1234...5678
AUTHORIZED_WALLET=none

# Client-side configuration (REQUIRED - must start with VITE_)
# This tells the frontend which Hyperliquid network to use
VITE_HYPERLIQUID_TESTNET=false

# =============================================================================
# OPTIONAL: Only needed if you want database features
# =============================================================================
# DATABASE_URL=mysql://user:password@host:3306/dbname

# =============================================================================
# OPTIONAL: Only needed if you want Google OAuth login
# (Not needed for MetaMask-only authentication)
# =============================================================================
# OAUTH_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# OAUTH_CLIENT_SECRET=your_google_client_secret
# OAUTH_REDIRECT_URI=https://your-domain.vercel.app/api/oauth/callback

# =============================================================================
# IMPORTANT NOTES FOR VERCEL:
# =============================================================================
# 1. Go to: https://vercel.com/[your-username]/[your-project]/settings/environment-variables
# 2. Add each variable above (one at a time)
# 3. Set scope to: Production, Preview, and Development
# 4. Click "Save"
# 5. Redeploy your app after adding variables
#
# CRITICAL: Make sure VITE_HYPERLIQUID_TESTNET is set!
# Without this, the frontend won't know which network to use.
# =============================================================================
